const category = require('../library/category/category.route')
const discount = require('../library/discount/discount.route')
const measurement = require('../library/measurement/measurement.route')

module.exports = {
    category,
    discount,
    measurement
}